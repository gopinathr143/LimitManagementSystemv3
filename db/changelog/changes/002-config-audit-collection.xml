<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:mongodb="http://www.liquibase.org/xml/ns/mongodb"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
        http://www.liquibase.org/xml/ns/mongodb
        http://www.liquibase.org/xml/ns/mongodb/liquibase-mongodb-latest.xsd">

    <!--
        BRD §4.4 "Change auditability" — immutable trail for every admin
        mutation on the clients registry (create / status change / timezone
        change / auth rotation). STORY-01-01 AC5 requires this trail to carry
        actor and timestamp.
    -->
    <changeSet id="001-create-config-audit-collection" author="imps-velocity">
        <mongodb:createCollection collectionName="configAudit">
            <mongodb:options>
                {
                  "validator": {
                    "$jsonSchema": {
                      "bsonType": "object",
                      "required": ["clientId", "resource", "action", "actor", "occurredAt"],
                      "properties": {
                        "clientId": { "bsonType": "string" },
                        "resource": { "enum": ["CLIENT"] },
                        "action": { "bsonType": "string" },
                        "actor": { "bsonType": "string" },
                        "before": { "bsonType": ["object", "null"] },
                        "after": { "bsonType": ["object", "null"] },
                        "occurredAt": { "bsonType": "date" }
                      }
                    }
                  },
                  "validationLevel": "strict",
                  "validationAction": "error"
                }
            </mongodb:options>
        </mongodb:createCollection>
    </changeSet>

    <changeSet id="002-config-audit-client-occurredat-idx" author="imps-velocity">
        <mongodb:createIndex collectionName="configAudit">
            <mongodb:keys>{ "clientId": 1, "occurredAt": -1 }</mongodb:keys>
            <mongodb:options>{ "name": "idx_configAudit_clientId_occurredAt" }</mongodb:options>
        </mongodb:createIndex>
    </changeSet>

</databaseChangeLog>
